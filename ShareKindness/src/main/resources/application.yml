server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sharekindness_db
    username: kindness
    password: kindness2024
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: bXktc3VwZXItc2VjcmV0LXNlY3JldC1rZXk= 
  expiration: 86400000 # Token validity in milliseconds (24 hours)
